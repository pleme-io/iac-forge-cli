name: release-binaries

# Thin wrapper around substrate/.github/workflows/rust-binary-release.yml.
# On every `v*` tag push, build the iac-forge binary for 4 platforms and
# attach it to the GitHub Release that softprops/action-gh-release creates.

on:
  push:
    tags: ['v*']

jobs:
  binaries:
    uses: pleme-io/substrate/.github/workflows/rust-binary-release.yml@main
    with:
      binary-name: iac-forge
      # All in-house backends except `pangea` (its upstream `arch-synthesizer`
      # transitive dep is broken — separate org issue tracked elsewhere). The
      # `pleme-io/actions/iac-forge` action accepts any of these via its
      # `backend:` input, so the published binary must support them all.
      features: terraform,pulumi,crossplane,ansible,steampipe,helm
      no-default-features: true
